export default function RegistrationForm({ registration }) {
  return (
    <form className="space-y-4">
      {/* fields: player_name, contact, link, tier, notes, user, character, status, reviewed_by, reviewed_at */}
    </form>
  )
}
