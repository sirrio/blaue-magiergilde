export default function AdminRegistrations({ registrations, filters }) {
  return (
    <div className="container mx-auto p-4">
      {/* Filters and search */}
      {/* Table of registrations with pagination */}
      {/* Bulk actions and Create button */}
    </div>
  )
}
